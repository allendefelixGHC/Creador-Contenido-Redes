---
phase: 07-carousel-publishing-ig-fb
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified: [n8n/workflow.json]
autonomous: false

must_haves:
  truths:
    - "A 5-slide carousel brief approved via WA results in a live IG carousel post with 5 slides in correct order"
    - "The same approval creates a live FB multi-photo post with 5 images"
    - "The n8n execution trace shows 30s Wait between child container creation and parent container POST"
    - "A 3-slide carousel brief produces attached_media[0..2] (not hardcoded to 5)"
    - "Single-post briefs still publish correctly to IG + FB (regression test)"
  artifacts:
    - path: "n8n/workflow.json"
      provides: "Deployed and verified carousel publish workflow"
  key_links:
    - from: "n8n-azure.propulsar.ai workflow"
      to: "Instagram + Facebook live posts"
      via: "Meta Graph API v22"
      pattern: "carousel.*publish"
---

<objective>
Deploy the carousel-enabled workflow to n8n-azure and run end-to-end carousel tests to verify the full pipeline works with real Meta API calls.

Purpose: Validate that the carousel publish chain works with real data — IG carousel 3-step flow, FB attached_media, WA notifications, and Sheets logging. Also verify single-post still works (regression).
Output: Deployed workflow on n8n-azure.propulsar.ai. Live carousel posts on IG + FB. E2E test results documented.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-carousel-publishing-ig-fb/07-RESEARCH.md
@.planning/phases/07-carousel-publishing-ig-fb/07-01-SUMMARY.md
@.planning/phases/07-carousel-publishing-ig-fb/07-02-SUMMARY.md
@n8n/workflow.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy workflow to n8n-azure and add Sheets columns</name>
  <files>n8n/workflow.json</files>
  <action>
**A. Deploy workflow.json to n8n-azure.propulsar.ai:**

Use the established deploy pattern from Phase 5/6:
1. Read the current workflow ID from n8n (should be the main content engine workflow)
2. PUT the updated workflow.json via n8n public API: `PUT https://n8n-azure.propulsar.ai/api/v1/workflows/{id}`
3. Activate: `PATCH https://n8n-azure.propulsar.ai/api/v1/workflows/{id}/activate`
4. Verify: `GET https://n8n-azure.propulsar.ai/api/v1/workflows/{id}` — check node count matches local (57 nodes), active=true

The n8n API key is available in .env as `N8N_API_KEY`. The workflow ID is the same one used in Phase 5/6 deployments (check prior summaries or query `GET /api/v1/workflows` to find it).

**B. Verify FACEBOOK_PAGE_ID and INSTAGRAM_ACCOUNT_ID env vars are set on n8n-azure:**

These should already be present from Phase 5/6. Verify by checking if the existing single-post publish still works (regression test in Task 2).

**C. Add Format and Num_Slides columns to Google Sheets:**

The carousel Sheets Log node writes `Format` and `Num_Slides` columns. Add these as headers in the "Publicaciones" sheet:
- Open the Google Sheet (ID in $env.GOOGLE_SHEETS_ID)
- Add column headers `Format` and `Num_Slides` after the last existing column
- This can be done via the Google Sheets API if accessible, or flagged for manual addition

If the Sheets API is not accessible, note this for the checkpoint in Task 2 — the user can add the columns manually before testing.
  </action>
  <verify>
Run: `curl -s -H "X-N8N-API-KEY: $N8N_API_KEY" https://n8n-azure.propulsar.ai/api/v1/workflows/{id} | python -c "import json,sys; w=json.load(sys.stdin); print('Nodes:', len(w['nodes']), 'Active:', w['active'])"` — should show 57 nodes, Active: True.
  </verify>
  <done>
Workflow deployed with 57 nodes, active on n8n-azure. Sheets columns ready for carousel data.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: E2E carousel tests (5-slide, 3-slide, single-post regression)</name>
  <files>n8n/workflow.json</files>
  <action>
Run three E2E tests against the deployed workflow on n8n-azure.propulsar.ai:

**Test A — 5-slide carousel (happy path):**
1. Run `node wizard/run.js` and create a carousel brief with 5 slides
2. Wait for WhatsApp preview (all 5 slide images + approval message)
3. Reply "SI" to approve
4. Expected: Live IG carousel (5 slides correct order) + FB multi-photo (5 images) + WA notification "Carrusel publicado (5 slides)" with IG permalink + FB URL + Sheets row with Format=carousel, Num_Slides=5

**Test B — 3-slide carousel (dynamic slide count):**
1. Run wizard, create a 3-slide carousel, approve via WA
2. Expected: Live IG carousel (3 slides) + FB post (3 images) + WA message "3 slides" — proves attached_media is dynamic

**Test C — Single-post regression:**
1. Run wizard, create a single-post, approve via WA
2. Expected: Live IG single post + FB single photo + WA notification with both URLs — proves format branch FALSE path works

**Key execution trace checks:**
- Test A: 30s Wait shows ~30 second duration; 5 child container POSTs; 5 unpublished photo POSTs; Build attached_media output has 5 media_fbid objects
- Test C: Format branch goes FALSE, existing single-post chain executes

**If attached_media fails (HTTP 400):** Change FB Publish Carousel Feed to specifyBody="string" and have Build attached_media output complete body as $json.request_body string. Redeploy and retest.

If any bugs are found, fix in workflow.json, redeploy, and retest until all 3 tests pass.
  </action>
  <verify>
Verify live posts on Instagram (instagram.com/propulsar_ai) and Facebook (Propulsar page). Check n8n execution history for all 3 tests. Verify WA notifications received. Verify Google Sheets rows.
  </verify>
  <done>
All 3 tests pass: 5-slide carousel, 3-slide carousel, single-post regression. Live posts visible on IG + FB. WA notifications received. Sheets rows logged with correct format and slide count.
  </done>
</task>

</tasks>

<verification>
1. Workflow deployed to n8n-azure with 57 nodes, active=true
2. Test A: 5-slide carousel live on IG + FB, correct order, WA notification received, Sheets row logged
3. Test B: 3-slide carousel live on IG + FB, proving dynamic attached_media
4. Test C: Single-post regression passes — IG + FB + WA + Sheets all work as before
5. Execution traces show 30s Wait, correct fan-out/aggregate patterns, retryOnFail=false on publish nodes
</verification>

<success_criteria>
- All three tests pass (5-slide carousel, 3-slide carousel, single-post regression)
- Live IG carousel posts visible with correct slide order
- Live FB multi-photo posts visible with all images
- WA notifications received with carousel-specific messaging
- Google Sheets rows logged with format and slide count
- No duplicate posts created during testing
- 30s Wait visible in execution trace for carousel tests
</success_criteria>

<output>
After completion, create `.planning/phases/07-carousel-publishing-ig-fb/07-03-SUMMARY.md`
</output>
