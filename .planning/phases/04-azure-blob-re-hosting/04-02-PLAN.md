---
phase: 04-azure-blob-re-hosting
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - n8n/workflow.json
autonomous: false

must_haves:
  truths:
    - "When a post is approved via SI in WhatsApp, the main workflow calls the Re-host Images sub-workflow BEFORE reaching any publishing/logging node"
    - "For single-image posts, exactly 1 blob URL is produced; for carousel posts (3-7 slides), N distinct blob URLs are produced in slide order"
    - "After re-hosting, the merged item downstream of '🔗 Merge Rehost Output' contains BOTH the original session fields (topic, type, angle, platforms, image_model, instagram, facebook, final_image_url, ...) AND the new blob_urls array, so Google Sheets Log has access to all original metadata alongside the new blob URLs"
    - "If the sub-workflow aborts (any image fails 3 retries), the main workflow execution fails and does NOT reach Google Sheets log or any Meta publish node"
    - "The rejection path (NO approval) is unchanged — it still routes to '❌ Loguear rechazo' with no re-host call"
    - "The pre-approval WhatsApp preview flow is unchanged — images are still previewed via their original Ideogram/FAL URLs before re-hosting"
  artifacts:
    - "n8n/workflow.json contains a new Execute Workflow node named '🔁 Re-host Images' wired after '🔍 Recuperar sesión Supabase'"
    - "n8n/workflow.json contains a new Code node ('🔧 Prep Re-host Input') that normalizes single-post and carousel data into the sub-workflow's expected input shape ({ image_urls, post_id, approval_number })"
    - "n8n/workflow.json contains a new Set node ('🔗 Merge Rehost Output') between '🔁 Re-host Images' and '📊 Google Sheets Log' that re-attaches all original session fields to the sub-workflow's output"
    - "n8n/workflow.json Google Sheets Log node consumes blob_urls[0].url (not final_image_url) for the Imagen_URL column, via an expression fallback"
  key_links:
    - "✅ ¿Aprobado? (true output) -> 🔍 Recuperar sesión Supabase -> 🔧 Prep Re-host Input -> 🔁 Re-host Images -> 🔗 Merge Rehost Output -> 📊 Google Sheets Log"
    - "Execute Workflow node workflowId param points to the imported sub-workflow's n8n ID (from Plan 01)"
    - "🔧 Prep Re-host Input Code node produces { image_urls: [{index, url}], post_id, approval_number } from BOTH single-post data (final_image_url) AND carousel data (image_urls array), passing all upstream session fields through unchanged"
    - "🔗 Merge Rehost Output Set node reads sub-workflow output ($json.blob_urls, $json.post_id) AND pulls original session fields from $('🔧 Prep Re-host Input').item.json to produce a merged item for downstream consumption"
---

<objective>
Integrate the re-host sub-workflow (built in Plan 01) into the main `n8n/workflow.json` so that the approved-post path calls it before any downstream publishing/logging, and wire the output blob URLs back into the main flow's execution data.

Purpose: Phase 4's success criteria require the re-host to happen on SI approval. This plan is the glue between the existing v1.0 approval flow and the new sub-workflow. Phases 5-7 will later consume `blob_urls` from the main flow's execution data.

Output: A modified `n8n/workflow.json` with 3 new nodes inserted between `🔍 Recuperar sesión Supabase` and `📊 Google Sheets Log` (`🔧 Prep Re-host Input`, `🔁 Re-host Images`, `🔗 Merge Rehost Output`), and one small update to the Sheets log expression to prefer the blob URL.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-azure-blob-re-hosting/04-CONTEXT.md
@.planning/phases/04-azure-blob-re-hosting/04-RESEARCH.md
@.planning/phases/04-azure-blob-re-hosting/04-01-SUMMARY.md
@n8n/workflow.json
@n8n/subworkflow-rehost-images.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Prep-Input + Execute-Workflow nodes into main workflow.json</name>
  <files>n8n/workflow.json</files>
  <action>
Modify `n8n/workflow.json` IN PLACE. Preserve all existing nodes, node IDs, and connections that are not explicitly touched below. Do NOT reformat unrelated parts of the JSON.

**Before editing:** Confirm with Felix (or from Plan 01's summary file `.planning/phases/04-azure-blob-re-hosting/04-01-SUMMARY.md`) the exact n8n workflow ID assigned to the imported sub-workflow. Call this value `<SUBFLOW_ID>` below — substitute the real ID when writing the node.

If Felix has not yet imported the sub-workflow or has not provided the ID, **STOP and ask for it** before editing. Do not guess.

**Change 1: Add three new nodes to the `nodes` array** (append to the end of the array, before the closing `]`):

Node A — **`🔧 Prep Re-host Input`** (Code node):
```json
{
  "parameters": {
    "jsCode": "// Normalize both single-post and carousel briefs into the sub-workflow's expected input shape.\n// Single post: $json has final_image_url (string).\n// Carousel: $json has image_urls (array of strings) and num_images.\n//\n// Output shape for Execute Workflow:\n//   { image_urls: [{ index, url }], post_id, approval_number }\n\nconst data = $input.first().json;\nconst format = data.format || 'single';\n\nlet imageUrls = [];\nif (format === 'carousel' && Array.isArray(data.image_urls)) {\n  imageUrls = data.image_urls.map((url, i) => ({ index: i + 1, url }));\n} else if (data.final_image_url) {\n  imageUrls = [{ index: 1, url: data.final_image_url }];\n} else {\n  throw new Error('Prep Re-host Input: no image URL found on approved post (neither final_image_url nor image_urls)');\n}\n\n// post_id: derive from session/timestamp. Prefer an existing session_id if present (set by Recuperar sesion Supabase).\nconst postId = data.session_id || ('propulsar_' + Date.now());\n\nreturn [{\n  json: {\n    // Pass ALL upstream fields through so downstream nodes (Sheets Log, future Meta publish)\n    // still have access to topic, type, angle, instagram/facebook text, etc.\n    ...data,\n    // Plus the sub-workflow input fields:\n    image_urls: imageUrls,\n    post_id: postId,\n    approval_number: data.approval_number,\n  }\n}];",
    "mode": "runOnceForAllItems"
  },
  "id": "prep-rehost-input",
  "name": "🔧 Prep Re-host Input",
  "type": "n8n-nodes-base.code",
  "typeVersion": 2,
  "position": [2400, 380],
  "notes": "Normalizes single-post (final_image_url) and carousel (image_urls) into the sub-workflow input shape { image_urls: [{index,url}], post_id, approval_number }. Passes all other fields through so downstream nodes retain topic/type/angle/text."
}
```

Node B — **`🔁 Re-host Images`** (Execute Workflow node):
```json
{
  "parameters": {
    "source": "database",
    "workflowId": {
      "__rl": true,
      "mode": "id",
      "value": "<SUBFLOW_ID>"
    },
    "workflowInputs": {
      "mappingMode": "passthrough"
    },
    "options": {
      "waitForSubWorkflow": true
    }
  },
  "id": "execute-rehost-subflow",
  "name": "🔁 Re-host Images",
  "type": "n8n-nodes-base.executeWorkflow",
  "typeVersion": 1,
  "position": [2620, 380],
  "notes": "Calls the Re-host Images sub-workflow. workflowInputs.mappingMode MUST be 'passthrough' because the sub-workflow trigger uses inputSource: passthrough — using 'defineBelow' with an empty value would send empty input and the sub-workflow's Explode node would throw. Output: { blob_urls, post_id }. NOTE: Execute Workflow replaces the item with the sub-workflow output, so downstream nodes no longer see topic/type/angle/etc. — that's why we need the '🔗 Merge Rehost Output' node next. If sub-workflow throws StopAndError, this node fails and the main execution aborts — Google Sheets Log and any Meta publish nodes will NOT run."
}
```

Node C — **`🔗 Merge Rehost Output`** (Set node — preferred over Code because it avoids any jsCode and therefore any `$env` concern):
```json
{
  "parameters": {
    "mode": "manual",
    "duplicateItem": false,
    "assignments": {
      "assignments": [
        {
          "id": "merge-blob-urls",
          "name": "blob_urls",
          "type": "array",
          "value": "={{ $json.blob_urls }}"
        },
        {
          "id": "merge-post-id",
          "name": "post_id",
          "type": "string",
          "value": "={{ $json.post_id }}"
        },
        {
          "id": "merge-topic",
          "name": "topic",
          "type": "string",
          "value": "={{ $('🔧 Prep Re-host Input').item.json.topic }}"
        },
        {
          "id": "merge-type",
          "name": "type",
          "type": "string",
          "value": "={{ $('🔧 Prep Re-host Input').item.json.type }}"
        },
        {
          "id": "merge-angle",
          "name": "angle",
          "type": "string",
          "value": "={{ $('🔧 Prep Re-host Input').item.json.angle }}"
        },
        {
          "id": "merge-platforms",
          "name": "platforms",
          "type": "array",
          "value": "={{ $('🔧 Prep Re-host Input').item.json.platforms }}"
        },
        {
          "id": "merge-image-model",
          "name": "image_model",
          "type": "string",
          "value": "={{ $('🔧 Prep Re-host Input').item.json.image_model }}"
        },
        {
          "id": "merge-instagram",
          "name": "instagram",
          "type": "object",
          "value": "={{ $('🔧 Prep Re-host Input').item.json.instagram }}"
        },
        {
          "id": "merge-facebook",
          "name": "facebook",
          "type": "object",
          "value": "={{ $('🔧 Prep Re-host Input').item.json.facebook }}"
        },
        {
          "id": "merge-final-image",
          "name": "final_image_url",
          "type": "string",
          "value": "={{ $('🔧 Prep Re-host Input').item.json.final_image_url }}"
        },
        {
          "id": "merge-format",
          "name": "format",
          "type": "string",
          "value": "={{ $('🔧 Prep Re-host Input').item.json.format }}"
        },
        {
          "id": "merge-approval-number",
          "name": "approval_number",
          "type": "string",
          "value": "={{ $('🔧 Prep Re-host Input').item.json.approval_number }}"
        }
      ]
    },
    "options": {}
  },
  "id": "merge-rehost-output",
  "name": "🔗 Merge Rehost Output",
  "type": "n8n-nodes-base.set",
  "typeVersion": 3.4,
  "position": [2840, 380],
  "notes": "Re-attaches original session metadata (from 🔧 Prep Re-host Input) to the sub-workflow output (blob_urls, post_id). Without this, Execute Workflow replaces the item with only { blob_urls, post_id }, and Google Sheets Log would write blank columns. Uses a Set node (no jsCode) to avoid any $env concern. If the Wizard brief ever adds new session fields, extend this node's assignments array accordingly."
}
```

**IMPORTANT on `workflowId`:** If `<SUBFLOW_ID>` format with `__rl` object does not work in the project's n8n version (2.14.2), try the simpler form:
```json
"workflowId": "<SUBFLOW_ID>"
```
n8n 2.14.2 may accept either depending on how Execute Workflow is saved. If unsure, Felix can wire this node manually in the n8n UI after import (drag the sub-workflow onto the Execute Workflow selector) and then export the workflow.json to capture the exact shape.

**Change 2: Modify the `connections` object:**

The existing chain is:
```
✅ ¿Aprobado? -> 🔍 Recuperar sesión Supabase -> 📊 Google Sheets Log
```

Rewire to:
```
✅ ¿Aprobado? -> 🔍 Recuperar sesión Supabase -> 🔧 Prep Re-host Input -> 🔁 Re-host Images -> 🔗 Merge Rehost Output -> 📊 Google Sheets Log
```

Specifically:

a) **Remove** the existing `🔍 Recuperar sesión Supabase` -> `📊 Google Sheets Log` connection. In the JSON, locate the `"🔍 Recuperar sesión Supabase": { "main": [[{ "node": "📊 Google Sheets Log", ... }]] }` block and replace the target node name with `"🔧 Prep Re-host Input"`.

b) **Add** a new connection entry:
```json
"🔧 Prep Re-host Input": {
  "main": [
    [
      { "node": "🔁 Re-host Images", "type": "main", "index": 0 }
    ]
  ]
}
```

c) **Add** another new connection entry:
```json
"🔁 Re-host Images": {
  "main": [
    [
      { "node": "🔗 Merge Rehost Output", "type": "main", "index": 0 }
    ]
  ]
}
```

d) **Add** another new connection entry:
```json
"🔗 Merge Rehost Output": {
  "main": [
    [
      { "node": "📊 Google Sheets Log", "type": "main", "index": 0 }
    ]
  ]
}
```

**Change 3: Update the Google Sheets Log expression** to prefer the blob URL over the original Ideogram/FAL URL.

Because `🔗 Merge Rehost Output` now sits immediately upstream of `📊 Google Sheets Log`, `$json` at the Sheets Log node contains BOTH the original session fields (topic, type, angle, platforms, instagram, facebook, final_image_url, ...) AND the new `blob_urls` array. All other Sheets Log column expressions continue to work unchanged because the merge node passes them through.

Locate the `📊 Google Sheets Log` node. In its `parameters.columns.value.Imagen_URL` field, change:
```
"Imagen_URL": "={{ $json.final_image_url || '' }}"
```
to:
```
"Imagen_URL": "={{ ($json.blob_urls && $json.blob_urls[0] && $json.blob_urls[0].url) || $json.final_image_url || '' }}"
```

This reads the blob URL from the merged data (provided by `🔗 Merge Rehost Output`) and falls back to the original Ideogram/FAL URL only as a defensive guard. Under normal Phase 4 flow, the blob URL will always be present because a re-host failure aborts the execution before reaching this node.

While auditing the Sheets Log node, also verify each existing column expression still resolves against the merged item — none should reference fields that the merge node fails to copy. If any column references a field NOT in the merge node's assignments list (e.g., a future field added by the Wizard), extend the merge node's assignments accordingly before proceeding.

**Change 4: Do NOT touch any of these:**
- The pre-approval preview branch (`📤 Enviar preview imagen`, `📱 Preparar mensaje WA`, `📤 Enviar WhatsApp`, WA webhook, YCloud reply)
- The rejection branch (`❌ Loguear rechazo` and its connection from `✅ ¿Aprobado?` output 1)
- The carousel split/preview loop (`🎠 Explode Slides`, `📨 Split URLs WA`, `📤 Enviar imagen WA`)
- The image generation branches (Flux, Ideogram, Nano Banana, Normalize URL)
- The `_meta` block at the bottom — leave the version as `"3.1"` (Plan 02 bumps it in Task 2, not here)

Commit message: `feat(phase-4): wire re-host sub-workflow into main approval path`
  </action>
  <verify>
1. `node -e "JSON.parse(require('fs').readFileSync('n8n/workflow.json','utf8'))"` succeeds (still valid JSON).
2. `grep -c '"name": "🔧 Prep Re-host Input"' n8n/workflow.json` returns 1.
3. `grep -c '"name": "🔁 Re-host Images"' n8n/workflow.json` returns 1.
4. `grep -c '"name": "🔗 Merge Rehost Output"' n8n/workflow.json` returns 1.
5. `grep -c 'executeWorkflow' n8n/workflow.json` returns at least 1.
6. `grep 'Imagen_URL' n8n/workflow.json` shows the updated expression containing `blob_urls[0]`.
7. No reference to `📊 Google Sheets Log` exists as a direct target of `🔍 Recuperar sesión Supabase` anymore (grep the connections block).
8. `grep '<SUBFLOW_ID>' n8n/workflow.json` returns ZERO matches (confirms the placeholder was substituted with the real n8n workflow ID from Plan 01's summary).
9. `grep '"mappingMode": "passthrough"' n8n/workflow.json` returns at least 1 match (confirms Execute Workflow node uses passthrough mode, NOT defineBelow).
10. Felix imports the updated workflow.json into n8n, activates it, and confirms the canvas shows the new chain: Recuperar sesión Supabase -> Prep Re-host Input -> Re-host Images -> Merge Rehost Output -> Google Sheets Log.
  </verify>
  <done>
Main workflow.json contains the three new nodes (🔧 Prep Re-host Input, 🔁 Re-host Images, 🔗 Merge Rehost Output) correctly wired into the post-approval chain, the Sheets log expression consumes blob_urls[0].url with a fallback, and no other nodes or connections were modified. JSON is valid and importable. The `<SUBFLOW_ID>` placeholder has been substituted with the real n8n workflow ID from Plan 01's summary.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: End-to-end verification — single post + 5-slide carousel</name>
  <files>(Felix runs the Wizard + inspects Azure Portal)</files>
  <action>
This task is the Phase 4 phase-level acceptance test. It exercises the full approved-post path for both post formats and verifies all 5 Phase 4 success criteria at once.

**Pre-flight:**
1. Confirm the updated `n8n/workflow.json` (Task 1) is imported and activated in n8n.
2. Confirm the sub-workflow from Plan 01 is imported and activated in n8n.
3. Confirm Felix's `AZURE_SAS_PARAMS`, `AZURE_STORAGE_ACCOUNT`, `AZURE_CONTAINER` env vars are set on the n8n Container App.

**Test A — Single-image post (Flux/Ideogram):**
1. Run `node wizard/run.js` from the project root.
2. Pick any trending topic, any type (educational recommended — uses Flux).
3. Choose a 1-platform target (e.g., Instagram only).
4. Accept the suggested image model (or pick Flux explicitly).
5. Wait for the preview to arrive on WhatsApp.
6. Reply `SI` to approve.
7. Observe the n8n execution history:
   - [ ] `🔧 Prep Re-host Input` ran and produced `image_urls: [{ index: 1, url: "https://..." }]`
   - [ ] `🔁 Re-host Images` invoked the sub-workflow successfully
   - [ ] The sub-workflow history shows 1 HTTP GET + 1 HTTP PUT + 1 HTTP HEAD, all 2xx
   - [ ] `📊 Google Sheets Log` ran and wrote a row
8. Open Azure Portal -> Storage Account -> Containers -> `AZURE_CONTAINER`. Confirm a new blob exists under `YYYY/MM/DD/<uuid>.jpg` (or `.png`). **(Success Criterion 1)**
9. Open the Google Sheets log. The `Imagen_URL` column for the new row contains an `https://<account>.blob.core.windows.net/...` URL. Paste it into a browser — returns 200 OK showing the image. **(Success Criterion 2)**
10. Wait 2+ hours (can be combined with Test B). Re-open the same URL — still 200 OK. **(Success Criterion 3)**

**Test B — 5-slide carousel:**
1. Run `node wizard/run.js` again.
2. Pick a topic, choose a type that generates a carousel (follow Wizard prompts to reach the carousel branch — typically educational with a specific slide-count trigger). If the Wizard does not expose carousel directly, POST the webhook manually with `format: "carousel", num_images: 5` per the brief JSON contract in CLAUDE.md.
3. Approve with SI.
4. Observe n8n execution history:
   - [ ] `🔧 Prep Re-host Input` produced `image_urls` array of length 5, indices 1..5
   - [ ] `🔁 Re-host Images` sub-workflow history shows 5 HTTP GET + 5 HTTP PUT + 5 HTTP HEAD all 2xx
   - [ ] Sub-workflow output `blob_urls` has 5 items, all with `https://<account>.blob.core.windows.net/...` URLs, sorted by index 1..5
5. Open Azure Portal -> container. Confirm 5 new blobs under today's date prefix. **(Success Criterion 4)**
6. Open each of the 5 URLs in a browser — all return 200 OK with the expected slide image in order.
7. Confirm the 5 URLs map 1:1 to the 5 original slides in the correct order (visual check against what the Wizard generated).

**Test C — Failure path (REQUIRED — locked abort policy):**

This test is mandatory because the partial-failure retry-then-abort behavior is a locked phase decision. Phase 4 cannot be marked complete unless this test passes.

1. Temporarily edit `AZURE_SAS_PARAMS` to an invalid value (e.g., change the `sig=` to a garbage string). Restart the n8n container app.
2. Run the Wizard, approve a post.
3. Expected: the sub-workflow's HTTP PUT retries 2-3x then fails, `📱 Notify Abort WA` sends the "Error al re-alojar imagen" message, `🛑 Stop And Error` halts execution.
4. Confirm ALL of the following (each must be checked off in the final summary):
   - [ ] Google Sheets Log was NOT written (no new row appended)
   - [ ] No blobs created in the Azure container under today's date prefix
   - [ ] WhatsApp abort notification received on `approval_number`, message includes the slide index
   - [ ] n8n execution history shows the main workflow ended in `error` state, not `success`
5. Restore the real `AZURE_SAS_PARAMS`, restart, re-verify Test A still works (smoke test that the rollback is clean).

**Verification of Success Criterion 5 (env vars via expressions, no $env in Code):**
Run: `grep -n 'process.env\|\\$env\\..*\\.blob\\.core' n8n/subworkflow-rehost-images.json`
- Expected: no matches inside any `jsCode` field. `$env` references should only appear in `url` / `value` / `jsonBody` expression fields of HTTP Request / Set nodes.
  </action>
  <verify>
All six checkboxes under Test A pass, all four under Test B pass, the 2-hour persistence check passes, AND all four checkboxes under Test C pass (Test C is mandatory, not optional). All 5 phase success criteria from `.planning/ROADMAP.md` are demonstrably true. The final task summary MUST include an explicit pass/fail line for each test (A, B, C) — none can be skipped or marked N/A.
  </verify>
  <done>
Phase 4 is functionally complete: approved posts (single and carousel) successfully re-host all their images to Azure Blob before any Meta call, abort cleanly on upload failure with WhatsApp notification, and the Sheets log records the permanent blob URLs. Felix confirms all test outputs match the expected behavior.
  </done>
</task>

</tasks>

<verification>
Overall Phase 4 verification (cross-plan):

1. Success Criterion 1: Single-post SI approval triggers re-host, blob appears in Azure. -> Test A step 8.
2. Success Criterion 2: Blob URL accessible from browser without auth, no expiry. -> Test A step 9.
3. Success Criterion 3: 2-hour persistence. -> Test A step 10.
4. Success Criterion 4: 5-slide carousel produces 5 distinct blob URLs before Meta call. -> Test B steps 4-7. (Note: this plan does NOT add any Meta call — Success Criterion 4 is interpreted as "5 blob URLs are present in execution data and the Sheets log row for the post references them", since Phases 5-7 are out of scope.)
5. Success Criterion 5: Env vars confirmed via {{ $env.* }} only, no process.env in Code. -> Test C final grep.
</verification>

<success_criteria>
- Main workflow.json modified with exactly 3 new nodes (🔧 Prep Re-host Input, 🔁 Re-host Images, 🔗 Merge Rehost Output) and 4 updated connection entries (rewire Recuperar sesión target + 3 new entries for the new nodes), nothing else.
- End-to-end test of single post + 5-slide carousel both produce permanent Azure blob URLs reachable from a browser.
- Abort path (SAS invalid) halts the main workflow cleanly with WhatsApp notification and no Sheets write.
- All 5 Phase 4 success criteria from ROADMAP.md verified by Felix during Test A + Test B + Test C.
</success_criteria>

<output>
After completion, create `.planning/phases/04-azure-blob-re-hosting/04-02-SUMMARY.md` recording:
- Screenshot (or description) of the n8n canvas showing the three new nodes wired in
- Timestamps of Test A, Test B, and Test C executions (one line per test, ISO 8601)
- Explicit PASS/FAIL line for each of Test A, Test B, and Test C
- The exact blob URLs used for the 2-hour persistence check, with both upload and re-fetch timestamps
- All four Test C checkboxes confirmed (no Sheets row, no blobs, WhatsApp received, execution in error state)
- Any deviations from the plan (e.g., n8n 2.14.2 quirks in the Execute Workflow node parameters)
- A note that Phase 4 is ready for Phases 5-7 to consume `$json.blob_urls` (from the merged item) downstream
</output>
