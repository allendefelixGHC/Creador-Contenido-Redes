---
phase: 08-scheduling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wizard/run.js
  - n8n/workflow.json
autonomous: true

must_haves:
  truths:
    - "Wizard presents publish-time prompt after brief summary, before send confirmation"
    - "Entering 'ahora' sends publish_at:'now' in the brief JSON"
    - "Entering 'hoy 15:30' sends a UTC ISO string in publish_at"
    - "Past time triggers Wizard warning and forces ahora or future time"
    - "24h+ future time triggers Wizard warning"
    - "n8n Code node guard routes publish_at='now' to immediate path"
    - "n8n Code node guard routes future publish_at to Wait node with computed seconds"
    - "n8n Code node guard routes past publish_at to immediate path (defense in depth)"
    - "publish_at field saved to Supabase in both single and carousel session saves"
  artifacts:
    - path: "wizard/run.js"
      provides: "Scheduling prompt (PASO 6) with CET/CEST to UTC conversion"
      contains: "madridLocalToUTC"
    - path: "n8n/workflow.json"
      provides: "4 new scheduling nodes + 2 updated Supabase saves"
      contains: "Compute wait_seconds"
  key_links:
    - from: "wizard/run.js"
      to: "n8n webhook"
      via: "publish_at field in brief JSON"
      pattern: "publish_at"
    - from: "n8n/workflow.json (Supabase save)"
      to: "n8n/workflow.json (Code guard)"
      via: "publish_at survives webhook hop through Supabase session"
      pattern: "publish_at"
    - from: "n8n/workflow.json (Code guard)"
      to: "n8n/workflow.json (Wait node)"
      via: "scheduled=true routes to Wait with wait_seconds"
      pattern: "wait_seconds"
    - from: "n8n/workflow.json (Wait node + Merge)"
      to: "n8n/workflow.json (Prep Re-host Input)"
      via: "Both paths reunite at Merge before rehosting"
      pattern: "Prep Re-host Input"
---

<objective>
Add publish scheduling to the Wizard and n8n workflow. The Wizard gets a new PASO 6 (publish-time prompt) with CET/CEST-to-UTC conversion and validation. The n8n workflow gets 4 new nodes (Code guard + IF router + Wait + Merge) inserted between Recuperar sesion Supabase and Prep Re-host Input, plus publish_at added to both Supabase session save nodes.

Purpose: Users can choose immediate or scheduled publishing (up to 24h ahead) with timezone-safe conversion and defense-in-depth guards.
Output: Updated wizard/run.js and n8n/workflow.json with full scheduling capability ready for deployment.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-scheduling/08-RESEARCH.md
@wizard/run.js
@n8n/workflow.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scheduling prompt + UTC conversion to Wizard</name>
  <files>wizard/run.js</files>
  <action>
Add two utility functions BEFORE the `runWizard()` function:

1. `madridLocalToUTC(localDateStr, localTimeStr)` — probe-based offset detection using `Intl.DateTimeFormat('en-US', { timeZone: 'Europe/Madrid' })`. See RESEARCH.md "Wizard Side: UTC Conversion" for the exact implementation. No external dependencies.

2. `parsePublishTime(input)` — parses "ahora", "hoy HH:MM", "manana HH:MM" / "mañana HH:MM". Returns `{ publish_at: 'now' }` or `{ publish_at: ISO_STRING, wait_minutes: N }` or `{ error: string }`. Validates: past time returns error, >24h returns error. See RESEARCH.md "Wizard Side: Input Parsing" for exact implementation.

Then add PASO 6 inside `runWizard()` between the RESUMEN block (line ~367) and the confirm prompt (line ~386). Specifically:

- BEFORE the `// RESUMEN` comment (line 367), insert the scheduling prompt block:
  ```
  // PASO 6: HORA DE PUBLICACION
  div();
  console.log(c("bright", "  PASO 6 — Hora de publicacion\n"));
  ```
- Present 3 options: [1] Ahora, [2] Hoy HH:MM, [3] Manana HH:MM
- If choice is 2 or 3, ask for time input, parse with `parsePublishTime()`, loop on error (allow "ahora" as escape)
- Set `publishAt` variable (default 'now') and `publishAtDisplay` for the summary
- In the RESUMEN block, add a line showing the publish time: `console.log(\`  ⏰ Publicar:    \${publishAtDisplay}\`)`
- In the `brief` object (line ~393), add `publish_at: publishAt` field

The scheduling prompt pattern should match the existing Wizard style (same `ask()`, `c()`, `div()` helpers). See RESEARCH.md "Complete Wizard Scheduling Prompt Block" for the full code block.

IMPORTANT: The `parsePublishTime` regex must handle both "manana" (no tilde) and "mañana" (with tilde) — use pattern `/^ma[nñ]ana/`.
  </action>
  <verify>
Run: `node -e "const { madridLocalToUTC, parsePublishTime } = require('./test-sched'); console.log(parsePublishTime('ahora')); console.log(parsePublishTime('hoy 18:00'));"` — but since the functions are inside wizard/run.js (not exported), verify by reading the file and confirming:
1. `madridLocalToUTC` function exists before `runWizard`
2. `parsePublishTime` function exists before `runWizard`
3. PASO 6 block exists between PASO 5 and RESUMEN
4. `publish_at` field exists in the `brief` object
5. Run `node -c wizard/run.js` to confirm no syntax errors
  </verify>
  <done>
Wizard has PASO 6 scheduling prompt. "ahora" produces publish_at:"now". "hoy HH:MM" produces UTC ISO string. Past time and >24h are rejected with error messages. publish_at included in brief JSON sent to webhook. No new npm dependencies.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add scheduling gate nodes to n8n workflow + update Supabase saves</name>
  <files>n8n/workflow.json</files>
  <action>
This task modifies the n8n workflow JSON to add 4 new scheduling nodes and update 2 existing Supabase session save nodes. Total node count goes from 57 to 61.

**Part A: Update Supabase session saves to include publish_at**

1. In node "💾 Guardar sesión Supabase" (id: save-session-supabase), modify the `jsonBody` expression to add `publish_at: $json.publish_at || 'now'` to the JSON.stringify object. Insert it after the `status: 'pending'` field.

2. In node "💾 Guardar sesión Supabase (Carousel)" (id: save-session-carousel), modify the `jsonBody` expression to add `publish_at: $json.publish_at || 'now'` to the JSON.stringify object. Insert it after the `status: 'pending'` field.

**Part B: Add 4 new nodes to the nodes array**

Insert these 4 nodes. Position them between x=2624 (Recuperar sesion) and x=3060 (Prep Re-host). Use x positions: 2780, 2840, 2900, 2960 (spread evenly). All at y=380 (same as the existing path).

Node 1: Code node "🕐 Compute wait_seconds"
- id: "compute-wait-seconds"
- type: n8n-nodes-base.code, typeVersion: 2
- position: [2780, 380]
- mode: "runOnceForAllItems"
- jsCode: The guard logic from RESEARCH.md "n8n Code Node Guard (SCHED-03)". Key logic: read `data.publish_at`, if not 'now' and diffMs > 65000 and <= 86400000 then scheduled=true + wait_seconds=Math.round(diffMs/1000). Return `{ ...data, scheduled: String(scheduled), wait_seconds }`.
- notes: "SCHED-03: Computes wait_seconds from publish_at. Routes to immediate if publish_at is 'now', past, <65s (n8n DB threshold), or >24h. 65s floor: n8n does not persist Wait < 65s to DB — container restart would lose execution."

Node 2: IF node "⏰ ¿Programado?"
- id: "check-scheduled"
- type: n8n-nodes-base.if, typeVersion: 1 (CRITICAL: must be v1, not v2)
- position: [2840, 380]
- conditions: `{ "string": [{ "value1": "={{ $json.scheduled }}", "value2": "true" }] }`
- notes: "SCHED-01: Routes scheduled=true to Wait path (output 0/TRUE), scheduled=false to immediate path (output 1/FALSE). Uses IF typeVersion 1 — v2/Switch v3 broken in n8n 2.14.2."

Node 3: Wait node "⏳ Wait — Scheduled Publish"
- id: "wait-scheduled-publish"
- type: n8n-nodes-base.wait, typeVersion: 1
- position: [2900, 280] (offset y up for visual clarity on the scheduled branch)
- parameters: `{ "amount": "={{ $json.wait_seconds }}", "unit": "seconds" }`
- notes: "SCHED-02: Pauses execution for wait_seconds (computed by 🕐 Compute wait_seconds). Uses 'After Time Interval' mode (amount + unit), NOT 'At Specified Time' (unreliable per n8n #14723). wait_seconds is a Number, not string."

Node 4: Merge node "🔀 Merge Schedule Paths"
- id: "merge-schedule-paths"
- type: n8n-nodes-base.merge, typeVersion: 3
- position: [2960, 380]
- parameters: `{ "mode": "chooseBranch", "output": "empty" }`
  NOTE: Actually, use mode "passThrough" so that whichever branch arrives passes through. The Merge node in n8n with mode "chooseBranch" waits for both inputs. Since only ONE path fires, use mode "passThrough" with `output: "input1"` as default BUT the issue is only one branch fires. 
  
  CORRECTION: Do NOT use a Merge node. Instead, wire BOTH the Wait node output AND the IF FALSE output directly to "🔧 Prep Re-host Input". n8n supports multiple input connections to the same node. This is simpler and matches the pattern used elsewhere in this workflow (multiple nodes connecting to the same downstream node). This eliminates Node 4 entirely — only 3 new nodes needed (61-1=60 total... actually 57+3=60 nodes).

**Part C: Rewire connections**

1. Change the existing connection from "🔍 Recuperar sesión Supabase" → "🔧 Prep Re-host Input" to: "🔍 Recuperar sesión Supabase" → "🕐 Compute wait_seconds"

2. Add: "🕐 Compute wait_seconds" → "⏰ ¿Programado?"

3. Add: "⏰ ¿Programado?" TRUE (output index 0) → "⏳ Wait — Scheduled Publish"

4. Add: "⏰ ¿Programado?" FALSE (output index 1) → "🔧 Prep Re-host Input"

5. Add: "⏳ Wait — Scheduled Publish" → "🔧 Prep Re-host Input"

So Prep Re-host Input receives from two paths: the FALSE/immediate path and the Wait/scheduled path. This is the standard n8n fan-in pattern.

**Visual flow:**
```
🔍 Recuperar sesión → 🕐 Compute wait_seconds → ⏰ ¿Programado?
                                                    ├─ TRUE → ⏳ Wait → 🔧 Prep Re-host
                                                    └─ FALSE ──────────→ 🔧 Prep Re-host
```

IMPORTANT constraints:
- IF node MUST be typeVersion 1 (v2 broken in n8n 2.14.2)
- Wait node MUST be typeVersion 1 (consistent with 2 existing Wait nodes)
- Code node MUST NOT use require(), fetch(), or $helpers (sandbox restriction)
- wait_seconds MUST be Number type (not string) for Wait node amount
- scheduled field MUST be String(boolean) for IF v1 string comparison
  </action>
  <verify>
1. Parse the workflow JSON: `node -e "const w=JSON.parse(require('fs').readFileSync('n8n/workflow.json','utf8')); console.log('nodes:', w.nodes.length); const names=w.nodes.map(n=>n.name); console.log('has compute:', names.includes('🕐 Compute wait_seconds')); console.log('has if:', names.includes('⏰ ¿Programado?')); console.log('has wait:', names.includes('⏳ Wait — Scheduled Publish'));"` — expect nodes: 60, all three true.

2. Verify connections: `node -e "const w=JSON.parse(require('fs').readFileSync('n8n/workflow.json','utf8')); const c=w.connections; console.log('retrieve->compute:', JSON.stringify(c['🔍 Recuperar sesión Supabase'])); console.log('compute->if:', JSON.stringify(c['🕐 Compute wait_seconds'])); console.log('if outputs:', JSON.stringify(c['⏰ ¿Programado?'])); console.log('wait->prep:', JSON.stringify(c['⏳ Wait — Scheduled Publish']));"` — verify the connection chain matches the visual flow above.

3. Verify Supabase saves include publish_at: `node -e "const w=JSON.parse(require('fs').readFileSync('n8n/workflow.json','utf8')); const single=w.nodes.find(n=>n.name==='💾 Guardar sesión Supabase'); const carousel=w.nodes.find(n=>n.name==='💾 Guardar sesión Supabase (Carousel)'); console.log('single has publish_at:', single.parameters.jsonBody.includes('publish_at')); console.log('carousel has publish_at:', carousel.parameters.jsonBody.includes('publish_at'));"` — both true.

4. Verify IF typeVersion: `node -e "const w=JSON.parse(require('fs').readFileSync('n8n/workflow.json','utf8')); const ifNode=w.nodes.find(n=>n.name==='⏰ ¿Programado?'); console.log('typeVersion:', ifNode.typeVersion);"` — must be 1.
  </verify>
  <done>
n8n workflow has 3 new scheduling nodes (Code guard + IF router + Wait). Both Supabase save nodes include publish_at. Connection chain: Recuperar sesion → Compute wait_seconds → Programado? → (TRUE: Wait → Prep Re-host) / (FALSE: Prep Re-host). All node versions match project constraints. publish_at survives the WhatsApp webhook hop via Supabase session.
  </done>
</task>

</tasks>

<verification>
1. `node -c wizard/run.js` — no syntax errors
2. Workflow JSON is valid: `node -e "JSON.parse(require('fs').readFileSync('n8n/workflow.json','utf8')); console.log('valid JSON')"` — prints "valid JSON"
3. Node count is 60 (57 existing + 3 new)
4. All new nodes use correct typeVersions (Code v2, IF v1, Wait v1)
5. publish_at field present in both Supabase save expressions
6. Connection chain is correct: Recuperar sesion → Compute → IF → Wait/Prep Re-host
7. Wizard brief JSON includes publish_at field
</verification>

<success_criteria>
- Wizard PASO 6 presents scheduling options (ahora / hoy HH:MM / manana HH:MM)
- "ahora" produces `publish_at: "now"` in brief
- "hoy 15:30" produces UTC ISO string (correctly handling CET/CEST via Intl probe)
- Past time and >24h are rejected with user-friendly Spanish error messages
- n8n Code guard computes wait_seconds with 65s floor and 24h ceiling
- n8n IF routes scheduled=true to Wait, scheduled=false to immediate
- Both paths converge at Prep Re-host Input
- publish_at survives Supabase session hop (saved in both single and carousel saves)
</success_criteria>

<output>
After completion, create `.planning/phases/08-scheduling/08-01-SUMMARY.md`
</output>
