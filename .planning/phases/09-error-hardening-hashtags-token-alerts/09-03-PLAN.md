---
phase: 09-error-hardening-hashtags-token-alerts
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - n8n/workflow.json
autonomous: false

must_haves:
  truths:
    - "Simulated Meta API failure sends WhatsApp with error code, message, fbtrace_id, and platform"
    - "After successful IG publish, first comment with hashtags appears on the post"
    - "Expired token simulation triggers specific WhatsApp alert mentioning Susana"
    - "Failed publish creates Google Sheets row with Publish_Status=failed and error message"
    - "After successful publish, Azure Blob files for the post are deleted (verified via 404 on blob URL)"
  artifacts:
    - path: "n8n/workflow.json"
      provides: "Deployed workflow on n8n-azure with all Phase 9 features active"
  key_links:
    - from: "n8n/workflow.json (local)"
      to: "n8n-azure.propulsar.ai (deployed)"
      via: "PUT /api/v1/workflows + PATCH activate"
      pattern: "deploy"
---

<objective>
Deploy the Phase 9 workflow to n8n-azure and run E2E tests covering all 5 success criteria: error notifications, hashtag comments, token-expired alerts, failure logging, and blob cleanup.

Purpose: Verify all Phase 9 features work end-to-end on the production n8n instance. This is the final phase of v1.1 — after this, the automatic publishing pipeline is complete.

Output: Deployed workflow on n8n-azure. E2E test results confirming all 5 success criteria. 09-03-SUMMARY.md.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-error-hardening-hashtags-token-alerts/09-RESEARCH.md
@.planning/phases/09-error-hardening-hashtags-token-alerts/09-01-SUMMARY.md
@.planning/phases/09-error-hardening-hashtags-token-alerts/09-02-SUMMARY.md
@n8n/workflow.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy workflow to n8n-azure and add Error_Msg column to Google Sheets</name>
  <files>n8n/workflow.json</files>
  <action>
  **A. Deploy to n8n-azure**

  Use the established deploy pattern (from Phase 5-8):

  1. Read the current workflow.json
  2. Strip `settings` down to `{ "executionOrder": "v1" }` only (n8n API rejects additional settings properties — lesson from Phase 7 Plan 03)
  3. PUT to `https://n8n-azure.propulsar.ai/api/v1/workflows/<WORKFLOW_ID>` with the stripped workflow JSON
  4. PATCH to activate: `https://n8n-azure.propulsar.ai/api/v1/workflows/<WORKFLOW_ID>/activate`
  5. Verify: GET the workflow back and confirm node count matches local (should be ~73 nodes)

  Get WORKFLOW_ID from the existing deployed workflow (check n8n API or prior summaries).

  **B. Add Error_Msg column to Google Sheets**

  The Google Sheets document needs an `Error_Msg` column added. Use the Google Sheets API or instruct the executor to add it.

  Actually — the Google Sheets node in n8n auto-creates columns on append if they don't exist (as long as the header row has them). The simpler approach: just ensure the first fail-log write creates the column. But to be safe, add the column header to the Sheets document programmatically or verify it exists after first test run.

  **C. Verify AZURE_SAS_PARAMS has delete permission**

  Before testing blob cleanup, verify the SAS token was updated (from Plan 01 Task 1). Run a test DELETE:
  ```bash
  curl -s -o /dev/null -w "%{http_code}" -X DELETE \
    "https://propulsarcontent.blob.core.windows.net/posts/test-verify-delete-perm.txt?$AZURE_SAS_PARAMS" \
    -H "x-ms-version: 2020-10-02" \
    -H "x-ms-date: $(date -u '+%a, %d %b %Y %H:%M:%S GMT')"
  ```
  Expected: 404 (blob doesn't exist). If 403 → SAS not updated, must fix before testing.
  </action>
  <verify>
  ```bash
  # Verify deployment
  curl -s "https://n8n-azure.propulsar.ai/api/v1/workflows/<WORKFLOW_ID>" \
    -H "X-N8N-API-KEY: $N8N_API_KEY" | python -c "
  import json, sys
  wf = json.load(sys.stdin)
  nodes = wf.get('nodes', [])
  print(f'Deployed nodes: {len(nodes)}')
  # Check error handler nodes exist
  node_ids = [n['id'] for n in nodes]
  for nid in ['parse-meta-error', 'check-token-expired', 'wa-token-expired', 'sheets-fail-log', 'delete-azure-blob']:
    status = 'OK' if nid in node_ids else 'MISSING'
    print(f'  {nid}: {status}')
  "
  ```
  </verify>
  <done>
  Workflow deployed to n8n-azure with all Phase 9 nodes active. Node count verified. Error_Msg column present in Google Sheets.
  </done>
</task>

<task type="auto">
  <name>Task 2: E2E tests — error paths, hashtag comment, blob cleanup</name>
  <files>n8n/workflow.json</files>
  <action>
  Run 4 E2E tests covering the 5 success criteria. Each test sends a brief via the webhook and verifies the outcome.

  **Test A: Happy path + hashtag comment + blob cleanup (Success Criteria 2 + 5)**

  1. Send a single-post brief via webhook (use test-webhook.js or curl)
  2. Wait for content generation + image generation + WA preview
  3. Approve via simulated YCloud webhook (SI)
  4. Verify in n8n execution trace:
     - `✂️ Extract Hashtags (Single)` ran and produced `hashtag_block` (non-empty)
     - `💬 IG: Post Hashtag Comment` ran with 200 response
     - Blob cleanup: `🧹 Extract Blob Names` + `🗑️ Delete Azure Blob` ran
  5. Verify on Instagram: the published post has a first comment containing hashtags
  6. Verify blob URL from execution returns 404 (deleted)
  7. Verify Google Sheets: new row with `Publish_Status=success`, `Error_Msg=""`
  8. Clean up: delete the test FB post via Graph API

  **Test B: Simulated Meta error — generic failure (Success Criteria 1 + 4)**

  To simulate a Meta API error without actually breaking the token:
  - Temporarily modify the IG Create Container URL in the deployed workflow to use an invalid `INSTAGRAM_ACCOUNT_ID` (e.g., append `_invalid` to the env var reference in the URL)
  - Send a brief, approve it
  - The IG container creation should fail with a Meta API error
  - Verify:
    - WhatsApp error message received with error_code, message, fbtrace_id, platform
    - Google Sheets row with Publish_Status=failed and Error_Msg populated
    - Blob cleanup ran (blobs deleted despite failure)
  - Restore the original URL after test

  ALTERNATIVE (less invasive): Use n8n execution API to inject a test execution with a fake error response. But this is complex. The URL modification approach is simpler and proven.

  ANOTHER ALTERNATIVE: Send a brief with an invalid image URL that will fail at re-host stage. But that's before the Meta calls — won't test the error handler.

  SIMPLEST APPROACH: Temporarily set a wrong `META_PAGE_TOKEN` env var value (add a character to invalidate it). This will cause ALL Meta calls to fail with OAuthException. Then:
  - Test B becomes: invalid token → should trigger code 190 path (NOTIF-03)
  - This covers both Test B and Test C

  But modifying env vars requires Container App restart (slow). Better: modify the URL in the deployed workflow JSON to point to a non-existent IG account ID.

  **REVISED Test B: Invalid IG account ID (generic error, not code 190)**

  Temporarily change `ig-create-container` URL from `{{ $env.INSTAGRAM_ACCOUNT_ID }}` to `999999999999` (hardcoded invalid ID). This will return a Meta error that is NOT code 190 (probably code 100 or 803 — invalid parameter). This tests the generic error path.

  Steps:
  1. GET current workflow from n8n API
  2. Modify `ig-create-container` URL to use hardcoded invalid ID
  3. PUT modified workflow
  4. Send brief, approve
  5. Verify WA error message (generic, with error_code != 190)
  6. Verify Sheets fail log row
  7. Verify blob cleanup
  8. Restore original URL (PUT original workflow back)

  **Test C: Simulated token expiry — code 190 (Success Criteria 3)**

  If Test B's error happens to be code 190 (unlikely for invalid ID), this is covered. Otherwise:
  - Modify the `META_PAGE_TOKEN` env var to an invalid value in Azure Container App
  - Send brief, approve
  - Verify WA message contains "Token Meta expirado — verificar que Susana sigue como admin de la página"
  - Verify Sheets fail log
  - Restore META_PAGE_TOKEN

  If modifying env vars is too slow/risky, SKIP Test C as a live E2E and verify it structurally:
  - Confirm the IF node `⚠️ ¿Token Expirado?` correctly routes on `is_token_expired: true`
  - Confirm the WA message body contains the exact Spanish string
  - Accept structural verification for code 190 path (same pattern as Phase 5 Plan 02 Test B for retry disabled)

  **Test D: Blob cleanup verification (covered by Test A)**
  Already covered in Test A (verify blob URLs return 404 after successful publish).

  **Summary of E2E tests:**
  - Test A: Happy path + hashtag comment + blob cleanup (live)
  - Test B: Invalid IG account ID → generic error path (live, with workflow URL modification)
  - Test C: Token expired path → structural verification (or live if env var change is feasible)

  **After all tests:** Delete any test posts from Facebook via Graph API. Clean up Supabase sessions.
  </action>
  <verify>
  For each test, check:
  1. n8n execution trace shows correct node execution path
  2. WhatsApp messages received (check YCloud logs or phone)
  3. Google Sheets rows present with correct Publish_Status and Error_Msg values
  4. For Test A: IG post has first comment with hashtags; blob URL returns 404
  5. For Test B: WA error message has error_code, message, fbtrace_id, platform
  </verify>
  <done>
  All E2E tests pass:
  - Test A: hashtag comment posted + blobs cleaned up after success
  - Test B: generic Meta error → WA notification + Sheets fail log + blob cleanup
  - Test C: code 190 path verified (structurally or live)
  All 5 success criteria confirmed. Test posts cleaned up.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Phase 9 features end-to-end</name>
  <action>Human verifies all Phase 9 features on phone and in Google Sheets</action>
  <what-built>Complete Phase 9: error handling, hashtag comments, token alerts, failure logging, and blob cleanup — deployed and tested on n8n-azure</what-built>
  <how-to-verify>
  1. Check WhatsApp for test messages — you should have received:
     - A success notification with IG/FB links (Test A)
     - An error notification with error code and details (Test B)
  2. Check Google Sheets (Log tab):
     - Test A row: Publish_Status=success, Error_Msg empty
     - Test B row: Publish_Status=failed, Error_Msg populated
  3. Check Instagram: the Test A post should have a first comment with hashtags
  4. Verify no orphaned blobs: try accessing the blob URLs from Test A — should return 404
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. Workflow deployed on n8n-azure with ~73 nodes
2. Test A (happy path): IG post published + first comment with hashtags + blobs deleted + Sheets row with success
3. Test B (error path): WA error notification sent + Sheets fail log row + blobs cleaned
4. Test C (code 190): Token expired alert with correct Spanish text sent (or structurally verified)
5. All test posts cleaned up from Facebook
6. No orphaned Azure Blobs
</verification>

<success_criteria>
All 5 Phase 9 success criteria from ROADMAP.md verified:
1. Meta API failure → WA message with error_code, message, fbtrace_id, platform
2. IG publish → first comment with hashtag block, caption remains clean
3. Expired token → specific WA alert mentioning Susana
4. Publish failure → Sheets row with Publish_Status=failed + error message
5. Post-publish/post-failure → Azure Blobs deleted
</success_criteria>

<output>
After completion, create `.planning/phases/09-error-hardening-hashtags-token-alerts/09-03-SUMMARY.md`
</output>
