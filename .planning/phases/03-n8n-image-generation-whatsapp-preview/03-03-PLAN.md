---
phase: 03-n8n-image-generation-whatsapp-preview
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: [n8n/workflow.json, scripts/test-webhook.js]
autonomous: false

must_haves:
  truths:
    - "Workflow JSON passes structural validation (all connections reference existing nodes, no orphan connections)"
    - "test-webhook.js carousel mode sends a valid carousel brief that exercises the full carousel path"
    - "Single-post test brief still works and reaches the approval gate"
    - "Approval flow (SI/NO) works identically for both carousel and single-post"
  artifacts:
    - path: "n8n/workflow.json"
      provides: "Complete workflow with carousel image generation loop and WhatsApp preview"
      contains: "split-carousel-slides"
    - path: "scripts/test-webhook.js"
      provides: "Updated test script with carousel test mode"
      contains: "carousel"
  key_links:
    - from: "🔄 Split Send WA (Done)"
      to: "📱 Preparar mensaje WA"
      via: "carousel path merges into existing approval flow"
      pattern: "Split Send WA.*Preparar mensaje WA"
    - from: "📱 Preparar mensaje WA"
      to: "📤 Enviar WhatsApp"
      via: "unchanged connection — both paths share this"
      pattern: "Preparar mensaje WA.*Enviar WhatsApp"
---

<objective>
Validate the complete workflow structure and update the test script for end-to-end carousel verification.

Purpose: Ensure the workflow JSON is structurally sound (no broken connections, correct node references), update the test script to exercise the carousel path, and verify both paths converge at the approval gate.

Output: Validated workflow.json, updated test-webhook.js with carousel test payload.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-n8n-image-generation-whatsapp-preview/03-01-SUMMARY.md
@.planning/phases/03-n8n-image-generation-whatsapp-preview/03-02-SUMMARY.md
@n8n/workflow.json
@scripts/test-webhook.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Structural validation of workflow JSON + fix any issues</name>
  <files>n8n/workflow.json</files>
  <action>
Write and run a validation script that checks:

1. **Valid JSON**: Parse the file
2. **All connection targets exist**: Every node referenced in `connections` must exist in `nodes` array
3. **No orphan nodes** in carousel path: Every carousel node (explode-carousel-slides, split-carousel-slides, ideogram-slide, collect-carousel-urls, split-wa-urls, split-send-wa, send-wa-image) must appear in at least one connection (as source or target)
4. **Loop back-edges exist**: 
   - "🔤 Ideogram — Slide" connects back to "🔄 Split Slides"
   - "📤 Enviar imagen WA" connects back to "🔄 Split Send WA"
5. **SplitInBatches have 2 outputs**: Both Split Slides and Split Send WA must have exactly 2 output arrays in connections
6. **Single-post path intact**: Verify the full chain from "🔧 Parsear contenido" → "🖼️ ¿Imagen propia?" → "🎨 Router" → image nodes → "🔗 Normalizar URL" → "📱 Preparar mensaje WA" → "📤 Enviar WhatsApp" exists
7. **Both paths reach approval gate**: "📱 Preparar mensaje WA" → "📤 Enviar WhatsApp" → ... → "✅ ¿Aprobado?"

Run the validation as a Node.js script via bash. If any check fails, fix the issue in workflow.json and re-validate.

Additionally, verify that the node positions don't overlap (no two carousel-branch nodes at the exact same [x,y]). If positions overlap, adjust the new nodes to reasonable non-overlapping positions. Suggested layout for carousel branch:
- Explode Slides: [1080, 520]
- Split Slides: [1300, 520]  
- Ideogram — Slide: [1520, 620]
- Collect Image URLs: [1740, 520]
- Split URLs WA: [1960, 620]
- Split Send WA: [2180, 620]
- Enviar imagen WA: [2400, 720]

Ensure no carousel node position collides with an existing single-post node position. Adjust Y coordinates down (+100 or +200) if there are collisions with existing nodes.
  </action>
  <verify>
Run: `node -e "const w=require('./n8n/workflow.json'); const c=w.connections; const ns=w.nodes.map(n=>n.name); const ok=[]; ok.push(['valid JSON', true]); ok.push(['loop1 back-edge', c['🔤 Ideogram — Slide']?.main?.[0]?.[0]?.node==='🔄 Split Slides']); ok.push(['loop2 back-edge', c['📤 Enviar imagen WA']?.main?.[0]?.[0]?.node==='🔄 Split Send WA']); ok.push(['split1 2-out', c['🔄 Split Slides']?.main?.length===2]); ok.push(['split2 2-out', c['🔄 Split Send WA']?.main?.length===2]); ok.push(['single-post normalizar', !!c['🔗 Normalizar URL imagen']]); ok.push(['both->preparar', !!c['🔄 Split Send WA']?.main?.[1]?.[0]]); ok.forEach(([k,v])=>console.log(k+':',v)); if(ok.some(([,v])=>!v)) process.exit(1);"` — all lines must show true.
  </verify>
  <done>Workflow JSON passes all structural validation checks. All nodes reachable, both loops have back-edges, both SplitInBatches have 2 outputs, single-post path intact, both paths converge at approval gate, no position collisions.</done>
</task>

<task type="auto">
  <name>Task 2: Update test-webhook.js with carousel test payload</name>
  <files>scripts/test-webhook.js</files>
  <action>
Read the existing `scripts/test-webhook.js` to understand the current test structure.

Ensure the carousel test mode sends a brief that will exercise the FULL carousel path including the new image generation loop. The carousel test payload must include:

```json
{
  "topic": "5 herramientas IA que todo negocio necesita en 2026",
  "type": "educational",
  "angle": "Herramientas prácticas con resultados medibles",
  "platforms": ["instagram", "facebook"],
  "format": "carousel",
  "num_images": 3,
  "image_model": "ideogram",
  "fal_model_id": null,
  "has_own_image": false,
  "image_url": null,
  "has_text_in_image": true,
  "approval_number": "APPROVAL_NUMBER_FROM_ENV",
  "timestamp": "ISO_TIMESTAMP"
}
```

Key points:
- Use `num_images: 3` (smallest practical carousel — cheaper for testing at $0.18 total)
- The `format: "carousel"` field triggers the carousel IF branch in n8n
- `image_model: "ideogram"` and `has_text_in_image: true` are mandatory for carousel
- Read approval_number from `.env` (process.env.WHATSAPP_APPROVAL_NUMBER)

If the test script already has a carousel mode (from Phase 1), update it to match this payload format. If not, add a `--carousel` flag.
  </action>
  <verify>
Run: `node scripts/test-webhook.js --help` or `node scripts/test-webhook.js --dry-run` (if supported) to verify the script loads without errors. Check that carousel payload is well-formed JSON.
  </verify>
  <done>test-webhook.js has a working carousel test mode that sends a 3-slide carousel brief with format=carousel, num_images=3, image_model=ideogram. Running it will trigger the full carousel path in n8n.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete carousel image generation + WhatsApp preview pipeline in n8n:
1. Ideogram generation loop (3 slides sequentially)
2. WhatsApp image sending loop (3 images sent individually)
3. Text summary with SI/NO approval prompt
4. Single-post path untouched
  </what-built>
  <how-to-verify>
**Step 1: Upload workflow to n8n**
- Import `n8n/workflow.json` into n8n (n8n-azure.propulsar.ai)
- Manually link the OpenAI credential to both GPT-4o nodes
- Activate the workflow

**Step 2: Test carousel path**
- Run `node scripts/test-webhook.js --carousel`
- Watch n8n execution log — verify:
  - The "🔀 ¿Carrusel?" IF routes to carousel branch (true)
  - GPT-4o generates 3 slide prompts
  - SplitInBatches loops 3 times through Ideogram (3 separate HTTP calls)
  - Collect Image URLs outputs array with 3 URLs
  - Second SplitInBatches sends 3 WhatsApp image messages
  - Final text message arrives with "🎠 3 imágenes generadas (carrusel)"
  - SI/NO prompt appears

**Step 3: Test approval**
- Reply "SI" on WhatsApp
- Verify the approval flow proceeds to Recuperar sesión Supabase / Google Sheets Log

**Step 4: Test single-post path (regression)**
- Run `node scripts/test-webhook.js` (without --carousel flag)
- Verify single-post flows through the original path unchanged
- Single image message + text summary + SI/NO approval works as before
  </how-to-verify>
  <resume-signal>Type "approved" if both carousel and single-post paths work, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. Workflow JSON passes all structural validation checks
2. test-webhook.js supports both single-post and carousel test modes
3. Human verified: carousel sends N images + summary to WhatsApp
4. Human verified: single-post path unchanged
5. Human verified: SI/NO approval works for both paths
</verification>

<success_criteria>
Both carousel and single-post paths work end-to-end in the live n8n environment: carousel generates N Ideogram images sequentially, sends each to WhatsApp, shows carousel summary, and the approval gate works identically for both formats. Phase 3 is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/03-n8n-image-generation-whatsapp-preview/03-03-SUMMARY.md`
</output>
