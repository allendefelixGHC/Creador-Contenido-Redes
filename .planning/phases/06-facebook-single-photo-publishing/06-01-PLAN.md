---
phase: 06-facebook-single-photo-publishing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - n8n/workflow.json
autonomous: true

must_haves:
  truths:
    - "workflow.json contains a new FB Publish Photo HTTP Request node with retryOnFail disabled"
    - "The FB publish node is wired between IG Get Permalink and Notify WhatsApp Success"
    - "The WhatsApp success message includes both IG permalink and FB URL in one message"
    - "The Google Sheets Log FB_URL column uses the FB post_id to construct the URL"
  artifacts:
    - path: "n8n/workflow.json"
      provides: "FB publish node + updated WA notification + updated Sheets Log"
      contains: "FB: Publish Photo"
  key_links:
    - from: "🔗 IG: Get Permalink"
      to: "🌐 FB: Publish Photo"
      via: "n8n connection in workflow.json"
      pattern: "FB: Publish Photo"
    - from: "🌐 FB: Publish Photo"
      to: "✅ Notify WhatsApp Success"
      via: "n8n connection in workflow.json"
      pattern: "Notify WhatsApp Success"
    - from: "🌐 FB: Publish Photo"
      to: "📊 Google Sheets Log"
      via: "cross-node reference for FB_URL"
      pattern: "FB: Publish Photo.*post_id"
---

<objective>
Add the Facebook single-photo publish node to workflow.json and update the WhatsApp notification and Google Sheets Log nodes to include the FB URL.

Purpose: Wire the complete FB publish chain locally before deploying to n8n-azure in Plan 02.
Output: Updated n8n/workflow.json with 1 new node, 2 updated nodes, and rewired connections.
</objective>

<execution_context>
@C:/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-facebook-single-photo-publishing/06-RESEARCH.md
@n8n/workflow.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FB Publish Photo node and rewire connections</name>
  <files>n8n/workflow.json</files>
  <action>
Read `n8n/workflow.json` and make the following changes:

**1. Add new node `🌐 FB: Publish Photo` to the `nodes` array:**

```json
{
  "parameters": {
    "method": "POST",
    "url": "=https://graph.facebook.com/v22.0/{{ $env.FACEBOOK_PAGE_ID }}/photos",
    "sendHeaders": true,
    "headerParameters": {
      "parameters": [
        { "name": "Content-Type", "value": "application/json" }
      ]
    },
    "sendBody": true,
    "specifyBody": "json",
    "jsonBody": "={{ JSON.stringify({ url: $('🔗 Merge Rehost Output').item.json.blob_urls[0].url, message: $('🔗 Merge Rehost Output').item.json.facebook_caption || $('🔗 Merge Rehost Output').item.json.instagram_caption || '', access_token: $env.META_PAGE_TOKEN }) }}",
    "options": {}
  },
  "id": "fb-publish-photo",
  "name": "🌐 FB: Publish Photo",
  "type": "n8n-nodes-base.httpRequest",
  "typeVersion": 4.2,
  "retryOnFail": false,
  "maxTries": 1,
  "waitBetweenTries": 0,
  "onError": "stopWorkflow",
  "position": [POSITION_AFTER_IG_GET_PERMALINK],
  "notes": "CRITICAL: Retry DISABLED (FBPUB-01, ERR-02 equivalent). POST /{PAGE_ID}/photos is NOT idempotent — retry after timeout where Meta already processed creates a duplicate live FB post. Input: blob_urls[0].url from Merge Rehost Output. Output: { id: <photo_id>, post_id: '<page_id>_<photo_id>' }. Post_id used to construct FB_URL as 'https://www.facebook.com/' + post_id."
}
```

Position the node by reading the position of `🔗 IG: Get Permalink` and placing it ~250px to the right on the same Y coordinate.

**2. Rewire connections in the `connections` object:**

- Find `"🔗 IG: Get Permalink"` in connections — change its target from `"✅ Notify WhatsApp Success"` to `"🌐 FB: Publish Photo"`
- Add new connection entry for `"🌐 FB: Publish Photo"` pointing to `"✅ Notify WhatsApp Success"` with `{"main": [[{"node": "✅ Notify WhatsApp Success", "type": "main", "index": 0}]]}`

**3. Update `✅ Notify WhatsApp Success` node's jsonBody:**

Replace the existing jsonBody expression with:
```
={{ JSON.stringify({
  from: $env.YCLOUD_WHATSAPP_NUMBER,
  to: $('🔗 Merge Rehost Output').item.json.approval_number,
  type: 'text',
  text: {
    body: '✓ Publicado en Instagram y Facebook\n\nTema: ' +
          $('🔗 Merge Rehost Output').item.json.topic +
          '\nInstagram: ' + $('🔗 IG: Get Permalink').item.json.permalink +
          '\nFacebook: https://www.facebook.com/' + $('🌐 FB: Publish Photo').item.json.post_id +
          '\nHora: ' + new Date().toISOString()
  }
}) }}
```

**4. Update `📊 Google Sheets Log` node's FB_URL column:**

Find the `FB_URL` field in the columns/values of the Google Sheets Log node. Change from `""` (empty string) to:
```
={{ 'https://www.facebook.com/' + $('🌐 FB: Publish Photo').item.json.post_id }}
```

**Important constraints:**
- The node ID should be a UUID (not "fb-publish-photo") — use the same format as other nodes in the workflow (e.g., lowercase hex UUID like the existing ones)
- `retryOnFail: false` and `maxTries: 1` are NON-NEGOTIABLE safety constraints — POST /{PAGE_ID}/photos is NOT idempotent
- Use `graph.facebook.com` (NOT graph.instagram.com) — per Phase 5 decision
- All cross-node references use `$('Node Name').item.json.field` pattern
  </action>
  <verify>
Run the following checks on the modified workflow.json:

1. `node -e "const w = require('./n8n/workflow.json'); const fb = w.nodes.find(n => n.name === '🌐 FB: Publish Photo'); console.log('FB node exists:', !!fb); console.log('retryOnFail:', fb.retryOnFail); console.log('maxTries:', fb.maxTries); console.log('typeVersion:', fb.typeVersion);"` — should output: exists true, retryOnFail false, maxTries 1, typeVersion 4.2

2. `node -e "const w = require('./n8n/workflow.json'); const c = w.connections; console.log('IG->FB:', JSON.stringify(c['🔗 IG: Get Permalink'])); console.log('FB->WA:', JSON.stringify(c['🌐 FB: Publish Photo']));"` — IG: Get Permalink should connect to FB: Publish Photo, FB: Publish Photo should connect to Notify WhatsApp Success

3. `node -e "const w = require('./n8n/workflow.json'); const wa = w.nodes.find(n => n.name === '✅ Notify WhatsApp Success'); console.log('WA has FB URL:', wa.parameters.jsonBody.includes('FB: Publish Photo'));"` — should output true

4. `node -e "const w = require('./n8n/workflow.json'); const sh = w.nodes.find(n => n.name === '📊 Google Sheets Log'); console.log('Sheets has FB URL:', JSON.stringify(sh.parameters).includes('FB: Publish Photo'));"` — should output true
  </verify>
  <done>
workflow.json contains the new FB Publish Photo node with retry disabled, connections rewired (IG Get Permalink → FB Publish Photo → Notify WhatsApp Success), WA notification includes both IG and FB URLs in one message, and Sheets Log populates FB_URL from post_id. All 4 verification checks pass.
  </done>
</task>

</tasks>

<verification>
- workflow.json is valid JSON (parseable by node -e)
- New node count = previous count + 1
- Connection chain: IG Get Permalink → FB Publish Photo → Notify WhatsApp Success → Google Sheets Log
- FB node has retryOnFail=false, maxTries=1
- WA notification body contains both "Instagram:" and "Facebook:" strings
- Sheets Log FB_URL references FB: Publish Photo post_id
</verification>

<success_criteria>
- workflow.json has 1 new HTTP Request node for FB publishing with retry disabled
- Connections rewired to insert FB publish between IG permalink and WA notification
- WA success message template includes both IG and FB URLs
- Google Sheets FB_URL column expression constructs URL from post_id
</success_criteria>

<output>
After completion, create `.planning/phases/06-facebook-single-photo-publishing/06-01-SUMMARY.md`
</output>
